# Unity Project Context

<!-- unity-onboarding:generated:start -->

Last analyzed: 2026-09-16

## Project summary

- **Confirmed:** Early Unity 2D project for the Ember prototype. The only authored scene before prototype work was `Assets/Scenes/SampleScene.unity`.
- **Confirmed:** Project root is `project Ember`.

## Environment

- Unity 6000.5.2f1.
- Universal Render Pipeline 17.6.0 with the 2D Renderer.
- Input System 1.19.0 is active (`activeInputHandler: 1`).
- Unity Test Framework 1.7.0 is installed.
- No networking implementation was found.

## Structure and architecture

- The project initially contained no first-party gameplay scripts or assembly definitions.
- Runtime prototype code belongs under `Assets/EmberPrototype/Runtime`.
- Editor-only prototype scene generation belongs under `Assets/EmberPrototype/Editor`.
- The prototype uses small, composable MonoBehaviours and procedural placeholder visuals.

## Scenes and startup

- `Assets/Scenes/SampleScene.unity` was the only enabled build scene at onboarding time.
- The Ember prototype uses `Assets/Scenes/EmberPrototype.unity` when generated by its editor builder.

## Validation and tooling

- No Unity MCP bridge or client configuration was detected.
- Repository inspection and Unity batch-mode compilation are the available validation paths.
- Unity Editor connection state and live Console inspection are unavailable without an MCP bridge.

## Constraints and unknowns

- Final art, animation, audio, gamepad controls, and production level structure are not established.
- Placeholder input and visuals are intentionally code-driven for the minimum prototype.

## Sources inspected

- `ProjectSettings/ProjectVersion.txt`
- `ProjectSettings/ProjectSettings.asset`
- `ProjectSettings/EditorBuildSettings.asset`
- `Packages/manifest.json`
- `Assets/Scenes/SampleScene.unity`

<!-- unity-onboarding:generated:end -->
